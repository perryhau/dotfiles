# ~/.tmuxinator/19wu.yml
# you can make as many tabs as you wish...

project_name: 19wu
project_root: ~/Documents/19wu
socket_name: 19wu # Not needed.  Remove to use default socket
rvm: ruby-1.9.3-p327
#pre: sudo /etc/rc.d/mysqld start
tabs:
  - all:
      layout: main-vertical
      #layout: even-horizontal
      panes:
        #- #empty, will just run plain bash
        - tail -f logs/development.log
        - zs
        - guard -i
  #- shell: git pull
  #- database: rails db
  #- server: rails s
  #- logs: tail -f logs/development.log
  #- console: rails c
  #- capistrano:
  #- server: ssh me@myhost
